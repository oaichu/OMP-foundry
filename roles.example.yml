# Copy the keys you want into ~/.omp/agent/config.yml → modelRoles:
# Then pick models in OMP /models → Roles. Do not edit plugin files.

modelRoles:
  default: provider/your-lead:high
  plan: provider/your-architect:max
  advisor: provider/your-principal:xhigh
  slow: provider/your-hardest:max
  task: provider/your-worker:high
  designer: provider/your-designer:high
  smol: provider/your-cheap:auto
